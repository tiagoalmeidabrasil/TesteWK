@model IEnumerable<TesteTecnicoWK_Web.Models.CategoriaViewModel>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<p>
    <a class="btn btn-success" asp-action="Create">Adicionar categoria</a>
</p>

<div class="row">
    <div class="box col-md-12">
        <div class="box-inner">
            <div class="box-header well" data-original-title="">
            </div>
            <div class="box-content">
                <div class="alert alert-info"><h3>Lista de categorias</h3></div>
                <table class="table table-striped table-bordered bootstrap-datatable datatable responsive">
                    <thead>
                        <tr>
                            <th>Nome</th>
                            <th>Descrição</th>
                            <th>Tipo</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Nome)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Descripcion)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Tipo)
                                </td>
                                <td class="center">
                                    <a class="btn btn-success" href="@Url.ActionLink("Edit", "CategoriasView", new { id = item.Id })">
                                        Editar
                                    </a>
                                     | 
                                    <a class="btn btn-info" href="@Url.ActionLink("Details", "CategoriasView", new { id = item.Id })">
                                        Detalhes
                                    </a>
                                     | 
                                    <a class="btn btn-danger" href="@Url.ActionLink("Delete", "CategoriasView", new { id = item.Id })">
                                        Deletar
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>

                </table>
            </div>
        </div>
    </div>
</div>
